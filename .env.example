REAL_GENERATION=false
MOCK_CHECKOUT=true
NEXT_PUBLIC_APP_URL=http://localhost:3000
# DATABASE_URL is required when persistent PostgreSQL repositories are enabled.
# DATABASE_URL=postgresql://user:password@localhost:5432/mongkol_lanna
# KIE_API_KEY must remain server-side and is ignored while REAL_GENERATION=false.
# KIE_API_KEY=
